<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>brick-module-ds</artifactId>
        <groupId>tech.gjsl</groupId>
        <version>${qdata.version}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>brick-module-ds-biz</artifactId>

    <description>
        system系统模块
    </description>

    <dependencies>
        <!-- 核心模块 config-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-config</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 系統模块 websocket-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-websocket</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 系统模块 brick-security -->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-security</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 系统模块 mybatis -->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-mybatis</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 系统模块 redis -->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-redis</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 鉴权模块 auth -->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-auth</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 通用工具-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-common</artifactId>
        </dependency>

        <!-- 文件上传-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-file</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-module-da-api</artifactId>
            <version>${qdata.version}</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-module-att-api</artifactId>
            <version>${qdata.version}</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-module-ds-api</artifactId>
            <version>${qdata.version}</version>
            <scope>compile</scope>
        </dependency>

    </dependencies>

</project>
