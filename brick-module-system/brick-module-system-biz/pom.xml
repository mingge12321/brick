<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>brick-module-system</artifactId>
        <groupId>tech.gjsl</groupId>
        <version>${qdata.version}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>brick-module-system-biz</artifactId>

    <description>
        system系统模块
    </description>

    <dependencies>
        <!-- 核心模块 config-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-config</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 系統模块 websocket-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-websocket</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 系统模块 qdata-security -->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-security</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 系统模块 mybatis -->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-mybatis</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- 系统模块 redis -->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-redis</artifactId>
            <version>${qdata.version}</version>
        </dependency>
<!-- ======================================================================== -->
        <!-- system-api模块 -->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-module-system-api</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-module-ds-api</artifactId>
            <version>${qdata.version}</version>
            <scope>compile</scope>
        </dependency>

        <!-- 定时任务-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-quartz</artifactId>
        </dependency>

        <!-- 代码生成-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-generator</artifactId>
        </dependency>

        <!-- 通用工具-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-common</artifactId>
        </dependency>

        <!-- 文件上传-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-file</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- pay-->
<!--        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-pay</artifactId>
            <version>${qdata.version}</version>
        </dependency>-->

        <!-- auth-->
        <dependency>
            <groupId>tech.gjsl</groupId>
            <artifactId>brick-auth</artifactId>
            <version>${qdata.version}</version>
        </dependency>

        <!-- swagger3-->
<!--        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-boot-starter</artifactId>
        </dependency>-->

        <!-- 防止进入swagger页面报类型转换错误，排除3.0.0中的引用，手动增加1.6.2版本 -->
<!--        <dependency>
            <groupId>io.swagger</groupId>
            <artifactId>swagger-models</artifactId>
            <version>1.6.2</version>
        </dependency>-->
    </dependencies>

</project>
